# Screenshots

Screenshots are stored in `public/screenshots` so they render in the README and can be reused in project pages.

## Current Screenshots

- `public/screenshots/subscribe.png`: main subscription page.

## Updating Screenshots

Run the app locally:

```bash
pnpm dev
```

Open the page in a browser and capture the updated state into `public/screenshots`.
